# Executor

## Че делает
Принимает AST дерево от парсера, проеряет семантику(существуют ли таблицы, совпадают ли типы)
Упаковывает данные в байты

### Вход:
`ASTNode` от парсера
### Выход:
Результат операции.
`SELECT` - JSON
`INSERT/UPDATE` - статус выполнения