-- незакрытый строковый литерал. изолирован в отдельном файле без DDL:
-- открытая кавычка без пары поглощает весь последующий ввод,
-- поэтому в общем батче и после неё в этом файле ничего ставить нельзя - всё будет съедено.

-- кавычка не закрыта: ; не находится, на EOF печатается
-- "incomplete statement (missing ';')", программа не падает
INSERT INTO t (s) VALUE ("hello
